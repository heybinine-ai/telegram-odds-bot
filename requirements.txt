services:
  - type: web
    name: telegram-odds-bot
    env: python
    region: virginia
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn main:app"
    envVars:
      - key: BOT_TOKEN
        sync: false
